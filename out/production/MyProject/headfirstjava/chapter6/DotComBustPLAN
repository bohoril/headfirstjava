Псевдокод для тестирования класса DotComBust

* Объявляем метод main.
* Объявляем переменную dotComBust и присваиваем ей экземпляр класса DotComBust.
* Вызываем метод setUpGame(), который создаст объекты DotCom, присвоит им имена и адреса, выведет краткую инструкцию.
* Проверяем размер ArrayList на наличие объектов.
* Проверяем размер ArrayList в каждом объекте на наличие значений (координат).
* Объявляем строковую переменную guess и присваиваем ей значение "A0".
* Создаем цикл в котором изменяем переменную guess и вызываем метод checkUserGuess(), передавая ему в качестве аргумента значение переменной guess.
* В цикле ведем счетчик, чтобы в дальнейшем сравнить его со значением переменной numOfGuesses.
* Завершаем цикл на попадании и пробуем вызвать метод checkUserGuess(), передавая ему в качестве аргумента тоже самое значение переменной guess.
* Проверяем что нам вернется информация о том, что в эту ячейку уже попадали.
* Проверяем что мы попали (вернулся результат со строкой "Попал").
* Ищем значение переменной guess в ArrayList объектов DotCom и убеждаемся в его отсутствии.
* Создаем такой же цикл и завершаем его на промахе.
* Проверяем что мы промахнулись (вернулся результат со строкой "Мимо").
* Создаем такой же цикл и завершаем его на потоплении сайта.
* Проверяем что сайт потоплен (вернулся результат со строкой "Потопил").
* Продолжаем цикл тестирования и завершаем его на потоплении всех сайтов.
* Проверяем результат.